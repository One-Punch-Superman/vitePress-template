### var,let,const 有什么区别

var 的核心特性
作用域：var 声明的变量是函数作用域或全局作用域
变量提升：声明被提升，初始化为 undefined
重复声明： 允许
可变性： 可以重新赋值

ES6 中 var、let、const 的核心差异：var 存在函数/全局作用域、变量提升及易泄漏问题；let 提供块级作用域并允许重新赋值，const 不可变引用但对象属性可变。建议优先使用 const，必要时用 let，彻底弃用 var，以提升代码健壮性与可维护性。
